# nx-builder

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-builder` to build the library.

## Running unit tests

Run `nx test nx-builder` to execute the unit tests via [Vitest](https://vitest.dev/).
