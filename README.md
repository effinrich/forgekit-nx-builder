# forgekit-reactor

MCP that scaffolds production-grade NX monorepos via an interactive wizard — NX + React 19 + Vite, Panda CSS + Ark UI, Storybook, and more, generated as deterministic, repeatable layers. Same inputs → same repo.

Status: early build (MVP in progress). Full README (install, usage, wizard walkthrough) lands in a later phase of this build.
